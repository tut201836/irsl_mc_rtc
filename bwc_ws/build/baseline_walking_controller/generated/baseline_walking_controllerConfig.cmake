
####### Expanded from @PACKAGE_INIT@ by configure_package_config_file() #######
####### Any changes to this file will be overwritten by the next CMake run ####
####### The input file was Config.cmake.in                            ########

get_filename_component(PACKAGE_PREFIX_DIR "${CMAKE_CURRENT_LIST_DIR}/../../../" ABSOLUTE)

macro(set_and_check _var _file)
  set(${_var} "${_file}")
  if(NOT EXISTS "${_file}")
    message(FATAL_ERROR "File or directory ${_file} referenced by variable ${_var} does not exist !")
  endif()
endmacro()

macro(check_required_components _NAME)
  foreach(comp ${${_NAME}_FIND_COMPONENTS})
    if(NOT ${_NAME}_${comp}_FOUND)
      if(${_NAME}_FIND_REQUIRED_${comp})
        set(${_NAME}_FOUND FALSE)
      endif()
    endif()
  endforeach()
endmacro()

####################################################################################

set(skip_this_file TRUE)
if(NOT baseline_walking_controller_FOUND)
  set(skip_this_file FALSE)
endif()
if(skip_this_file)
  foreach(component ${baseline_walking_controller_FIND_COMPONENTS})
    if(NOT "baseline_walking_controller_${component}_FOUND")
      set(skip_this_file FALSE)
    endif()
  endforeach()
endif()
if(skip_this_file)
  return()
endif()

set("baseline_walking_controller_INCLUDE_DIRS" "${PACKAGE_PREFIX_DIR}/include")
set("BASELINE_WALKING_CONTROLLER_INCLUDE_DIRS" "${PACKAGE_PREFIX_DIR}/include")
set("baseline_walking_controller_DOXYGENDOCDIR" "${PACKAGE_PREFIX_DIR}/share/doc/baseline_walking_controller/doxygen-html")
set("BASELINE_WALKING_CONTROLLER_DOXYGENDOCDIR" "${PACKAGE_PREFIX_DIR}/share/doc/baseline_walking_controller/doxygen-html")
set("baseline_walking_controller_DEPENDENCIES" "mc_rtc;baseline_footstep_planner")
set("baseline_walking_controller_PKG_CONFIG_DEPENDENCIES" "")

set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} )

# Find absolute library paths for all _PKG_CONFIG_LIBS as CMake expects full paths, while pkg-config does not.
set(_PACKAGE_CONFIG_LIBRARIES "")
set("_baseline_walking_controller_PKG_CONFIG_LIBDIR" "/mc_rtc_ws/install/lib")
set("_baseline_walking_controller_PKG_CONFIG_LIBS_LIST" "")
if(_baseline_walking_controller_PKG_CONFIG_LIBS_LIST)
  string(FIND ${_baseline_walking_controller_PKG_CONFIG_LIBS_LIST} ", " _is_comma_space)
  while(_is_comma_space GREATER -1)
    string(REPLACE ", " "," _baseline_walking_controller_PKG_CONFIG_LIBS_LIST "${_baseline_walking_controller_PKG_CONFIG_LIBS_LIST}")
    string(FIND ${_baseline_walking_controller_PKG_CONFIG_LIBS_LIST} ", " _is_comma_space)
  endwhile()
  string(REPLACE " " ";" _baseline_walking_controller_PKG_CONFIG_LIBS_LIST "${_baseline_walking_controller_PKG_CONFIG_LIBS_LIST}")
  set(LIBDIR_HINTS ${_baseline_walking_controller_PKG_CONFIG_LIBDIR})
  foreach(component ${_baseline_walking_controller_PKG_CONFIG_LIBS_LIST})
    string(STRIP ${component} component)
    # If the component is a link directory ("-L/full/path"), append to LIBDIR_HINTS.
    string(FIND ${component} "-L" _is_library_dir)
    if(${_is_library_dir} EQUAL 0)
      string(REGEX REPLACE "^-L" "" lib_path ${component})
      list(APPEND LIBDIR_HINTS "${lib_path}")
      continue()
    endif()
    # If the component is a library name
    string(FIND ${component} "-l" _is_library_name)
    if(${_is_library_name} EQUAL 0)
      string(REGEX REPLACE "^-l" "" lib ${component})
      find_library(abs_lib_${lib} ${lib} HINTS ${LIBDIR_HINTS})
      if(NOT abs_lib_${lib})
        IF(_LIBDIR_HINTS)
          message(STATUS "${lib} searched on ${_LIBDIR_HINTS} not FOUND.")
        ELSE()
          message(STATUS "${lib} not FOUND.")
        ENDIF()
      else()
        IF(_LIBDIR_HINTS)
          message(STATUS "${lib} searched on ${_LIBDIR_HINTS} FOUND. ${lib} at ${abs_lib_${lib}}")
        ELSE()
          message(STATUS "${lib} FOUND. ${lib} at ${abs_lib_${lib}}")
        ENDIF()
        list(APPEND _PACKAGE_CONFIG_LIBRARIES "${abs_lib_${lib}}")
      endif()
      unset(abs_lib_${lib} CACHE)
      continue()
    endif()
    # If the component contains a collection of additional arguments
    string(FIND ${component} "," _is_collection)
    if(${_is_collection} GREATER -1)
      string(REPLACE "," ";" component_list "${component}")
      list(GET component_list -1 lib_info)
      set(options ${component})
      list(REMOVE_AT options -1)
      string(FIND ${lib_info} "-l" _is_library_name)
      if(${_is_library_name} GREATER -1)
        string(REGEX REPLACE "^-l" "" lib ${lib_info})
        find_library(abs_lib_${lib} ${lib} HINTS ${LIBDIR_HINTS})
        if(NOT abs_lib_${lib})
          IF(_LIBDIR_HINTS)
            message(STATUS "${lib} searched on ${_LIBDIR_HINTS} not FOUND.")
          ELSE()
            message(STATUS "${lib} not FOUND.")
          ENDIF()
        else()
          IF(_LIBDIR_HINTS)
            message(STATUS "${lib} searched on ${_LIBDIR_HINTS} FOUND. ${lib} at ${abs_lib_${lib}}")
          ELSE()
            message(STATUS "${lib} FOUND. ${lib} at ${abs_lib_${lib}}")
          ENDIF()
          list(APPEND _PACKAGE_CONFIG_LIBRARIES "${abs_lib_${lib}}")
        endif()
        unset(abs_lib_${lib} CACHE)
        continue()
      else() # This is an absolute lib
        list(APPEND _PACKAGE_CONFIG_LIBRARIES "${component}")
      endif()
      continue()
    endif()
    # Else, this is just an absolute lib
    if(EXISTS "${component}")
      list(APPEND _PACKAGE_CONFIG_LIBRARIES "${component}")
    endif()
  endforeach()
endif(_baseline_walking_controller_PKG_CONFIG_LIBS_LIST)

set("baseline_walking_controller_LIBRARIES" ${_PACKAGE_CONFIG_LIBRARIES})
set("BASELINE_WALKING_CONTROLLER_LIBRARIES" ${_PACKAGE_CONFIG_LIBRARIES})


include("${CMAKE_CURRENT_LIST_DIR}/cxx-standard.cmake")



include(CMakeFindDependencyMacro)
if(${CMAKE_VERSION} VERSION_LESS "3.15.0")
  find_package(mc_rtc REQUIRED)
  find_package(baseline_footstep_planner REQUIRED)
else()
  find_dependency(mc_rtc REQUIRED)
  find_dependency(baseline_footstep_planner REQUIRED)
endif()

IF(COMMAND ADD_REQUIRED_DEPENDENCY)
  FOREACH(pkgcfg_dep ${baseline_walking_controller_PKG_CONFIG_DEPENDENCIES})
    # Avoid duplicated lookup.
    LIST (FIND $_PKG_CONFIG_REQUIRES "${pkgcfg_dep}" _index)
    IF(${_index} EQUAL -1)
      ADD_REQUIRED_DEPENDENCY(${pkgcfg_dep})
    ENDIF()
  ENDFOREACH()
ENDIF(COMMAND ADD_REQUIRED_DEPENDENCY)

include("${CMAKE_CURRENT_LIST_DIR}/baseline_walking_controllerTargets.cmake")

foreach(component ${baseline_walking_controller_FIND_COMPONENTS})
  set(comp_file "${CMAKE_CURRENT_LIST_DIR}/${component}Config.cmake")
  if(EXISTS ${comp_file})
    include(${comp_file})
  else()
    set(baseline_walking_controller_${component}_FOUND FALSE)
  endif()
  if(baseline_walking_controller_${component}_FOUND)
    message(STATUS "baseline_walking_controller: ${component} found.")
  else()
    if(baseline_walking_controller_FIND_REQUIRED_${component})
      message(FATAL_ERROR "baseline_walking_controller: ${component} not found.")
    else()
      message(STATUS "baseline_walking_controller: ${component} not found.")
    endif()
  endif()
endforeach()
check_required_components("baseline_walking_controller")

set(BWC_STATES_LIBRARIES "/mc_rtc_ws/install/lib/mc_controller/baseline_walking_controller/states")
set(BWC_STATES_FILES "${BWC_STATES_LIBRARIES}/data")
CHECK_MINIMAL_CXX_STANDARD(11 ENFORCE)
